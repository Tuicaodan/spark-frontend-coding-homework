import tw from "twin.macro";
import styled from "styled-components";

export const NoResourcesPageContainer = styled.div`
  ${tw`
        flex
        flex-col
        w-full
        h-full
        justify-center
        text-center
        font-bold
        text-[3em]
    `}
`;